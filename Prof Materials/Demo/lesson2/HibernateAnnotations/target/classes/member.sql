CREATE TABLE MEMBER (
	ID	BIGINT(20) NOT NULL AUTO_INCREMENT,
    MEMBERNUMBER    INT,
    AGE         VARCHAR(10),
    FIRSTNAME         VARCHAR(25),
    LASTNAME          VARCHAR(25),
    TITLE          VARCHAR(10),
    PRIMARY KEY (ID)
);
