CREATE TABLE VEHICLE (
	ID	BIGINT(20) NOT NULL AUTO_INCREMENT,
    VEHICLE_NO    VARCHAR(10)    NOT NULL,
    COLOR         VARCHAR(10),
    WHEEL         INT,
    SEAT          INT,
    PRIMARY KEY (ID)
);